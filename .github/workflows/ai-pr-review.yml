name: AI PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches:
      - main
  workflow_dispatch:
    inputs:
      pr_number:
        description: PR number to review
        required: true
        type: string
      ai_review_engine:
        description: AI review engine
        required: false
        default: ""
        type: string

permissions:
  contents: read
  pull-requests: write
  issues: write

concurrency:
  group: ai-pr-review-${{ github.event.pull_request.number || inputs.pr_number }}
  cancel-in-progress: true

jobs:
  ai-pr-review:
    name: AI PR Review
    runs-on: ubuntu-24.04
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.pull_request.draft == false }}

    env:
      PR_NUMBER: ${{ github.event.pull_request.number || inputs.pr_number }}
      AI_REVIEW_ENGINE_INPUT: ${{ github.event_name == 'workflow_dispatch' && inputs.ai_review_engine || '' }}
      AI_REVIEW_ENGINE: ${{ vars.AI_REVIEW_ENGINE || '' }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      ANTHROPIC_AUTH_TOKEN: ${{ secrets.DEEPSEEK_AUTH_TOKEN }}
      QODER_PERSONAL_ACCESS_TOKEN: ${{ secrets.QODER_PERSONAL_ACCESS_TOKEN }}
      QODERCLI_VERSION: "1.0.34"
      QODERCLI_CACHE_DIR: .github/.cache/qodercli

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Resolve AI review config
        id: ai-review-config
        shell: bash
        run: |
          set -euo pipefail
          config_json="$(python3 .github/workflows/scripts/ai_review_pr.py --resolve-config)"
          python3 - "$config_json" <<'PY'
          import json
          import os
          import sys

          config = json.loads(sys.argv[1])
          enabled = "true" if config["enabled"] else "false"
          engine = config["engine"]

          with open(os.environ["GITHUB_ENV"], "a", encoding="utf-8") as env_file:
              env_file.write(f"AI_REVIEW_ENGINE={engine}\n")
              env_file.write(f"AI_REVIEW_EFFECTIVE_ENABLED={enabled}\n")
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as output_file:
              output_file.write(f"engine={engine}\n")
              output_file.write(f"enabled={enabled}\n")
          print(f"AI review enabled: {enabled}")
          print(f"Selected AI review engine: {engine}")
          PY

      - name: Show AI review engine
        shell: bash
        run: |
          set -euo pipefail
          echo "AI review enabled: ${AI_REVIEW_EFFECTIVE_ENABLED}"
          echo "Selected AI review engine: ${AI_REVIEW_ENGINE}"

      - name: Restore qodercli cache
        if: ${{ steps.ai-review-config.outputs.enabled == 'true' && steps.ai-review-config.outputs.engine == 'qoder' }}
        id: qodercli-cache-restore
        uses: actions/cache/restore@v4
        with:
          path: ${{ env.QODERCLI_CACHE_DIR }}
          key: qodercli-${{ runner.os }}-${{ runner.arch }}-${{ env.QODERCLI_VERSION }}

      - name: Install qodercli
        if: ${{ steps.ai-review-config.outputs.enabled == 'true' && steps.ai-review-config.outputs.engine == 'qoder' && steps.qodercli-cache-restore.outputs.cache-hit != 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$QODERCLI_CACHE_DIR"
          npm install -g "@qoder-ai/qodercli@${QODERCLI_VERSION}" --prefix "$GITHUB_WORKSPACE/$QODERCLI_CACHE_DIR"

      - name: Save qodercli cache
        if: ${{ steps.ai-review-config.outputs.enabled == 'true' && steps.ai-review-config.outputs.engine == 'qoder' && steps.qodercli-cache-restore.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@v4
        with:
          path: ${{ env.QODERCLI_CACHE_DIR }}
          key: ${{ steps.qodercli-cache-restore.outputs.cache-primary-key }}

      - name: Add qodercli to PATH
        if: ${{ steps.ai-review-config.outputs.enabled == 'true' && steps.ai-review-config.outputs.engine == 'qoder' }}
        shell: bash
        run: echo "$GITHUB_WORKSPACE/$QODERCLI_CACHE_DIR/bin" >> "$GITHUB_PATH"

      - name: Check qodercli version
        if: ${{ steps.ai-review-config.outputs.enabled == 'true' && steps.ai-review-config.outputs.engine == 'qoder' }}
        shell: bash
        run: |
          set -euo pipefail
          actual_version="$(qodercli --version)"
          echo "qodercli version: ${actual_version}"
          test "${actual_version}" = "${QODERCLI_VERSION}"

      - name: Run AI PR review
        shell: bash
        run: |
          set -euo pipefail
          python3 .github/workflows/scripts/ai_review_pr.py

      - name: Post PR review comment
        if: ${{ always() }}
        shell: bash
        run: |
          set -euo pipefail
          if [[ ! -f .tmp/ai-review/review.md ]]; then
            echo "AI review body was not produced." >&2
            exit 1
          fi
          bash scripts/upsert-pr-comment.sh \
            "<!-- dogsquard-ai-code-review -->" \
            .tmp/ai-review/review.md
